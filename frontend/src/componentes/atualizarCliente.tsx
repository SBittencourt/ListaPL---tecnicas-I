import React from "react";
import { Link } from "react-router-dom";

type Props = {
    tema: string;
};

const Atualizar: React.FC<Props> = ({ tema }) => {
    return (
        <div>
            
        </div>
    );
};

export default Atualizar;
